public enum ElementType
{
    Container, Spacing, Element,
}

public enum ElementDirection
{
    None, Horizontal, Vertical,
}

public enum ElementSizes
{
    Default, Contain, Cover,
}

public enum ESize
{
    Default, Contain, Cover,
}

public enum UIColor
{
    DefaultPanel, HighlightPanel, ActivePanel,
    DefaultElement, ActiveElement,
    DefaultText, ActiveText,
}
